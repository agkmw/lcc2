# Task for worker-2

1. In this workspace, create `math_utils.py` with a function `add(a, b)` that returns the sum.
2. Verify it by running `python3 -c "from math_utils import add; print(add(20, 22))"`.
3. Report the exact command output in your result file.
