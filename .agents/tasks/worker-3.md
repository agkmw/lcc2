# Task for worker-3

1. In this workspace, create `inventory.md` listing every top-level directory you can see (excluding `.git`).
2. Add one line per directory describing what it appears to contain.
