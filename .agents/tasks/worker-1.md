# Task for worker-1

1. In this workspace, create a file `hello.py` containing a program that prints: `Hello from worker-1`.
2. Run it with `python3 hello.py`.
3. Report the exact command output in your result file.
